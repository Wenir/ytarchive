[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "tests"
version = "1.0.0"
description = "Tests"
readme = ""
requires-python = ">=3.7"
dependencies = []

[project.scripts]
tests = "main:main"